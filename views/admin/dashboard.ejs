<%- include('../layouts/admin', { title: title, user: user }) %>

<div class="container py-4">
    <!-- Header -->
    <div class="row mb-4">
        <div class="col-12">
            <h1 class="fw-bold">Dashboard Admin</h1>
            <p class="text-muted">Selamat datang di panel administrasi ByzantiumEdu</p>
        </div>
    </div>

    <!-- Statistics Cards -->
    <div class="row mb-5">
        <div class="col-md-2 mb-3">
            <div class="card bg-primary text-white">
                <div class="card-body text-center">
                    <i class="fas fa-users fa-2x mb-3"></i>
                    <h3 class="fw-bold"><%= stats.totalUsers %></h3>
                    <p class="mb-0">Total Pengguna</p>
                </div>
            </div>
        </div>
        <div class="col-md-2 mb-3">
            <div class="card bg-success text-white">
                <div class="card-body text-center">
                    <i class="fas fa-book fa-2x mb-3"></i>
                    <h3 class="fw-bold"><%= stats.totalCourses %></h3>
                    <p class="mb-0">Total Kursus</p>
                </div>
            </div>
        </div>
        <div class="col-md-2 mb-3">
            <div class="card bg-info text-white">
                <div class="card-body text-center">
                    <i class="fas fa-newspaper fa-2x mb-3"></i>
                    <h3 class="fw-bold"><%= stats.totalPosts %></h3>
                    <p class="mb-0">Total Kegiatan</p>
                </div>
            </div>
        </div>
        <div class="col-md-2 mb-3">
            <div class="card bg-warning text-white">
                <div class="card-body text-center">
                    <i class="fas fa-encyclopedia fa-2x mb-3"></i>
                    <h3 class="fw-bold"><%= stats.totalPedia %></h3>
                    <p class="mb-0">Total Artikel</p>
                </div>
            </div>
        </div>
        <div class="col-md-2 mb-3">
            <div class="card bg-danger text-white">
                <div class="card-body text-center">
                    <i class="fas fa-file-pdf fa-2x mb-3"></i>
                    <h3 class="fw-bold"><%= stats.totalEbooks %></h3>
                    <p class="mb-0">Total Ebook</p>
                </div>
            </div>
        </div>
        <div class="col-md-2 mb-3">
            <div class="card bg-secondary text-white">
                <div class="card-body text-center">
                    <i class="fas fa-chalkboard-teacher fa-2x mb-3"></i>
                    <h3 class="fw-bold"><%= stats.totalInstructors %></h3>
                    <p class="mb-0">Total Instruktur</p>
                </div>
            </div>
        </div>
    </div>

    <!-- Quick Actions -->
    <div class="row mb-5">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h5 class="fw-bold mb-0">Aksi Cepat</h5>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-2 mb-3">
                            <a href="/admin/courses/create" class="btn btn-primary w-100">
                                <i class="fas fa-plus me-2"></i>Tambah Kursus
                            </a>
                        </div>
                        <div class="col-md-2 mb-3">
                            <a href="/admin/posts/create" class="btn btn-info w-100">
                                <i class="fas fa-plus me-2"></i>Tambah Kegiatan
                            </a>
                        </div>
                        <div class="col-md-2 mb-3">
                            <a href="/admin/pedia/create" class="btn btn-warning w-100">
                                <i class="fas fa-plus me-2"></i>Tambah Artikel
                            </a>
                        </div>
                        <div class="col-md-2 mb-3">
                            <a href="/admin/ebooks/create" class="btn btn-danger w-100">
                                <i class="fas fa-plus me-2"></i>Tambah Ebook
                            </a>
                        </div>
                        <div class="col-md-2 mb-3">
                            <a href="/admin/instructors/create" class="btn btn-secondary w-100">
                                <i class="fas fa-plus me-2"></i>Tambah Instruktur
                            </a>
                        </div>
                        <div class="col-md-2 mb-3">
                            <a href="/admin/users" class="btn btn-dark w-100">
                                <i class="fas fa-users me-2"></i>Kelola Users
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Recent Content -->
    <div class="row">
        <!-- Recent Courses -->
        <div class="col-lg-6 mb-4">
            <div class="card">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h5 class="fw-bold mb-0">Kursus Terbaru</h5>
                    <a href="/admin/courses" class="btn btn-sm btn-outline-primary">Lihat Semua</a>
                </div>
                <div class="card-body">
                    <% if (recentCourses && recentCourses.length > 0) { %>
                        <% recentCourses.forEach(course => { %>
                            <div class="d-flex align-items-center mb-3">
                                <div class="flex-shrink-0">
                                    <% if (course.thumbnail) { %>
                                        <img src="<%= course.thumbnail %>" alt="<%= course.title %>" class="rounded" style="width: 50px; height: 50px; object-fit: cover;">
                                    <% } else { %>
                                        <div class="bg-light rounded d-flex align-items-center justify-content-center" style="width: 50px; height: 50px;">
                                            <i class="fas fa-book text-muted"></i>
                                        </div>
                                    <% } %>
                                </div>
                                <div class="flex-grow-1 ms-3">
                                    <h6 class="mb-1"><%= course.title %></h6>
                                    <small class="text-muted">
                                        <i class="fas fa-user me-1"></i><%= course.instructor_name %>
                                        <span class="ms-2">
                                            <i class="fas fa-calendar me-1"></i><%= new Date(course.created_at).toLocaleDateString('id-ID') %>
                                        </span>
                                    </small>
                                </div>
                                <div class="flex-shrink-0">
                                    <% if (course.is_published) { %>
                                        <span class="badge bg-success">Diterbitkan</span>
                                    <% } else { %>
                                        <span class="badge bg-warning">Draft</span>
                                    <% } %>
                                </div>
                            </div>
                        <% }) %>
                    <% } else { %>
                        <p class="text-muted text-center py-3">Belum ada kursus</p>
                    <% } %>
                </div>
            </div>
        </div>

        <!-- Recent Posts -->
        <div class="col-lg-6 mb-4">
            <div class="card">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h5 class="fw-bold mb-0">Kegiatan Terbaru</h5>
                    <a href="/admin/posts" class="btn btn-sm btn-outline-info">Lihat Semua</a>
                </div>
                <div class="card-body">
                    <% if (recentPosts && recentPosts.length > 0) { %>
                        <% recentPosts.forEach(post => { %>
                            <div class="d-flex align-items-center mb-3">
                                <div class="flex-shrink-0">
                                    <% if (post.featured_image) { %>
                                        <img src="<%= post.featured_image %>" alt="<%= post.title %>" class="rounded" style="width: 50px; height: 50px; object-fit: cover;">
                                    <% } else { %>
                                        <div class="bg-light rounded d-flex align-items-center justify-content-center" style="width: 50px; height: 50px;">
                                            <i class="fas fa-image text-muted"></i>
                                        </div>
                                    <% } %>
                                </div>
                                <div class="flex-grow-1 ms-3">
                                    <h6 class="mb-1"><%= post.title %></h6>
                                    <small class="text-muted">
                                        <i class="fas fa-calendar me-1"></i><%= new Date(post.created_at).toLocaleDateString('id-ID') %>
                                    </small>
                                </div>
                                <div class="flex-shrink-0">
                                    <% if (post.status === 'published') { %>
                                        <span class="badge bg-success">Diterbitkan</span>
                                    <% } else { %>
                                        <span class="badge bg-warning">Draft</span>
                                    <% } %>
                                </div>
                            </div>
                        <% }) %>
                    <% } else { %>
                        <p class="text-muted text-center py-3">Belum ada kegiatan</p>
                    <% } %>
                </div>
            </div>
        </div>
    </div>
</div> 